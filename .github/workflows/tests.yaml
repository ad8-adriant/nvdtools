name: Tests
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - name: Test
        run: go test -v ./...
